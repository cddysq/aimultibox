# AIMultiBox 后端配置

# AI 模式：local（本地 LaMa）或 cloud（云端 SDXL）
AI_MODE=local

# Replicate API Token（仅 cloud 模式需要）
# 获取地址：https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# 服务器配置
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS 配置
CORS_ORIGINS_STR=http://localhost:5173,http://localhost:3000
