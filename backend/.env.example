# AIMultiBox 后端配置
#
# 配置加载优先级：环境变量 > .env 文件 > 代码默认值
# 生产环境部署前请确认所有配置项

# ==================== 环境配置 ====================
# 生产环境应设置 APP_ENV=production, DEBUG=false

# 运行环境：development / production / testing
APP_ENV=development
# 调试模式（生产环境应设为 false）
DEBUG=true
# 监听地址（生产环境应设为 0.0.0.0）
HOST=127.0.0.1
# 监听端口
PORT=8000

# ==================== 可选功能 ====================
# 空值表示禁用对应功能

# Google OAuth 客户端 ID（空则禁用 Google 登录）
# 获取地址：https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=

# Replicate API Token（空则禁用云端模式）
# 获取地址：https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# ==================== 固定配置 ====================
# 有合理默认值，通常不需要修改

# API 路由前缀
API_PREFIX=/api
# CORS 允许的源（逗号分隔）
CORS_ORIGINS_STR=http://localhost:5173,http://127.0.0.1:5173

# AI 模式：local（本地 LaMa）/ cloud（云端 SDXL）
AI_MODE=local

# 会话有效期（天）
AUTH_SESSION_DAYS=30
# Cookie 名称
AUTH_COOKIE_NAME=aimb_session
# 仅 HTTPS 传输 Cookie（生产环境应设为 true）
AUTH_COOKIE_SECURE=false
# Cookie 跨站策略：strict / lax / none
AUTH_COOKIE_SAMESITE=lax
